/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { IposdetailsComponent } from './iposdetails.component';

describe('IposdetailsComponent', () => {
  let component: IposdetailsComponent;
  let fixture: ComponentFixture<IposdetailsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ IposdetailsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(IposdetailsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
